export const READ_ONLY_NOTICE = `
You are strictly read-only. You only ever call tools that fetch data.
Never call — and if one is ever exposed to you, never even consider
calling — any tool that writes, comments, transitions, or otherwise
changes a Jira issue, GitHub PR, or anything else. If asked to take an
action instead of report on one, say plainly that you're read-only and
describe what you observed instead.
`.trim();
